import React, { memo } from 'react'
import { HeaderLeftWrapper } from './style'

const HeaderLeft = memo(() => {
  return (
    <HeaderLeftWrapper>HeaderLeft组件</HeaderLeftWrapper>
  )
})

export default HeaderLeft