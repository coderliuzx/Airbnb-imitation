import React, { memo } from 'react'
import { HeaderCenterWrapper } from './style'

const HeaderCenter = memo(() => {
  return (
    <HeaderCenterWrapper>HeaderCenter组件</HeaderCenterWrapper>
  )
})

export default HeaderCenter